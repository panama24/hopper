
const generateBookEnds = () => {
  // choose # movements (m)
  // pattern: ['m1, m2, m3, m2, m1', 'm1, m2, m3, m2, m3, m1']
  const repScheme = '';
  const movements = [];
  return {
    repScheme,
    movements,
  }
};

export {
  generateBookEnds,
}